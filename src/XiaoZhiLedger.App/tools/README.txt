FFmpeg 可选工具目录
===================

如果希望软件开箱即用地生成代表帧、鼠标扫片缓存和兼容预览，可将同一版本的：

  ffmpeg.exe
  ffprobe.exe

放在本目录中。

也可以在软件右上角点击“设置 FFmpeg”，选择电脑上已有的 ffmpeg.exe；
如果 FFmpeg 已加入系统 PATH，软件也会自动识别。

FFmpeg 不影响账本、项目、状态、备注、拖拽、复制和目录监控功能。
